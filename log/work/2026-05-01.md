# 2026-05-01 工作日誌 (MusiCodeStudio)

## 今日進度
1. **專案定名與更名**：確立專案名稱為 **MusiCodeStudio** (移除連字號以利編程)，並完成目錄更名。
2. **核心架構確立**：確立「Web UI (Vite/React/TS) + Native Engine (C++/JUCE/Tracktion Engine)」雙層分離架構。
3. **後端環境搭建**：
   - 成功下載 Tracktion Engine 原始碼及 JUCE 子模組。
   - 解決 CMake 版本衝突 (VS 2026 搭配 VS 17 2022 產生器)。
   - 建立專屬後端專案 `MusiCodeEngine`，並在 VS 2026 中編譯成功。
4. **前端介面開發**：
   - 初始化 Vite + React + TS 專案。
   - 實作 **Studio Paper** 淺色系風格，建立類 Waveform 橫向訊號流佈局。
   - 實作三重視圖原型：**五線譜 (OSMD)**、**鋼琴捲軸 (Konva)**、**積木 (Blockly)**。
5. **通訊對接 (IPC)**：
   - 定義前端 `EngineService` 及其 JSON 指令格式。
   - 在 C++ 引擎中實作基礎 Socket 監聽，成功偵測到網頁連線並實現視覺化反饋 (UI CONNECTED!)。

## 檔案結構異動
- `MusiCodeStudio/` (根目錄)
  - `DESIGN.md`: 專案設計計畫書。
  - `engine/MusiCodeEngine/`: 專屬 C++ 音訊引擎原始碼。
  - `ui/`: 前端 React 專案原始碼。
  - `log/work/2026-05-01.md`: 詳細開發紀錄。

## 待辦事項 (Next)
- 實作 C++ 端完整的 WebSocket 握手協定 (Handshake)，建立穩定連線。
- 整合 Tracktion Engine 播放邏輯至 `MusiCodeEngine`。
- 實作前端 Piano Roll 音符拖拽與實時 MIDI 同步。
