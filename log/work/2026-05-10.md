# 2026-05-10 工作日誌 (MusiCode Engine 核心優化與重構)

## 本次工作摘要
- **JSON 指令映射系統實作**：
    - 在 `HttpServer` 中導入 `std::map` 指令映射機制，大幅提升擴充性。
    - 指令協議完全對齊前端 `EngineService.ts`（使用 `action` 作為 Key）。
    - 支援指令：`transport_play`, `transport_stop`, `set_bpm`。
- **Tracktion Engine API 安全化**：
    - 修正內建插件建立流程，改用 `te::FourOscPlugin::xmlTypeName` 常數避免硬編碼錯誤。
    - 修正 `tracktion::` 命名空間導致的編譯錯誤（TimePosition, BeatPosition）。
- **表驅動管理維護**：
    - 更新 `AudioEngine_FunctionsTable.html` 以反映最新的指令處理邏輯。

## 技術細節異動
- **HttpServer.h/cpp**: 
    - 導入 `CommandHandler` 映射表。
    - 增加 `registerHandlers()` 進行指令註冊。
- **AudioEngine.cpp**:
    - 插件插入邏輯對齊 TE 12+ API 規範。
    - 修正命名空間解析問題。

## 待辦事項更新
- [ ] 整合系統音訊設備選擇器 (AudioDeviceSelector)。
- [ ] 規劃合成器積木 (DSP) 的 JSON 描述協議。
