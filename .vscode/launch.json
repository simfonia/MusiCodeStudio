{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch UI (npm dev)",
            "type": "node-terminal",
            "request": "launch",
            "command": "npm run dev",
            "cwd": "${workspaceFolder}/ui",
            "console": "integratedTerminal"
        }
    ]
}
